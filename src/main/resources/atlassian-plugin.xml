<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="i18n" location="prom-confluence-exporter"/>
    
    <!-- add our web resources -->
    <web-resource key="prom-confluence-exporter-resources" name="prom-confluence-exporter Web Resources">
        <dependency>com.atlassian.auiplugin:ajs</dependency>
        
        <resource type="download" name="prom-confluence-exporter.css" location="/css/prom-confluence-exporter.css"/>
        <resource type="download" name="prom-confluence-exporter.js" location="/js/prom-confluence-exporter.js"/>
        <resource type="download" name="images/" location="/images"/>

        <context>prom-confluence-exporter</context>
    </web-resource>
    
</atlassian-plugin>